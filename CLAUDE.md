# local_llm — 로컬 LLM 공부 저장소

RTX 5090 32GB 한 대로 **원문 커리큘럼 21단계를 순서대로 완주**하는 것이 목표입니다.
학습자는 소프트웨어 개발 경험은 있으나 **LLM / GPU / 딥러닝 배경은 없습니다.**

## 디렉터리 역할

| 경로 | 역할 |
|---|---|
| `README.md` | **원문 커리큘럼 21단계.** 이 저장소의 기준 문서 — 고치지 않습니다 |
| `study/` | 단계별 **개념 이해서** (마크다운 원본 + HTML 산출물) |
| `study/GUIDE.md` | **자료 작성 기준.** `study/` 를 건드리기 전에 반드시 먼저 읽습니다 |
| `study/build/` | 마크다운 → HTML 빌드 도구 (단계 무관, 재사용) |
| `ollama/` | 2–3단계 실행 환경 (docker-compose + 평가 하네스). 아직 실습 전 |

## 스터디 자료를 쓸 때

**`study/GUIDE.md` 를 먼저 읽으세요.** 아래는 그중 자주 어기는 것만 뽑은 요약입니다.

- 자료는 **개념 이해서**입니다. 명령어 실행과 결과 기록은 실행 환경 디렉터리(`ollama/` 등)의 몫입니다
- 모든 개념 절에 `[완전]` / `[씨앗]` 을 붙이고, `[씨앗]` 절은 `::: later` 로 **어느 단계에 넘기는지 + 지금 알아야 할 것은 어디까지인지** 밝히고 끝냅니다
- 뒤 단계가 직접 측정할 결론을 미리 쓰지 않습니다 (선점 금지)
- 모든 수치는 **RTX 5090 32GB × 1 기준**으로 재계산합니다. 원문의 3090 24GB × 2 전제는 `::: quote` 인용으로만 씁니다
- `.md` 가 원본입니다. **`.html` 을 직접 편집하지 않습니다**

### 자료를 만드는 단계는 정해져 있습니다

**21단계 전부에 자료를 만들지 않습니다.** 확정된 범위는 `study/GUIDE.md` 11절이고, 요약은 이렇습니다.

| 만드는 자료 | 단계 | 상태 |
|---|---|---|
| `01-hardware.md` | 1 | ✅ 완료 |
| `02-03-ollama-quant.md` | 2–3 | ✅ 완료 |
| `04-06-vllm-serving.md` | 4–6 | ✅ 완료 |
| `07-kv-cache.md` | 7 | ✅ 완료 |
| `08-09-context-parallel.md` | 8–9 | ✅ 완료 |
| `10-lora.md` | 10 | ✅ 완료 |
| `16-layer-quant.md` | 16 | ✅ 완료 |

- **만들지 않음**: 11 · 12 · 13 (도구·설계 판단 → 실행 환경 README), 14 · 17 (소스 읽기·커널 작성이 본체), 18 · 19 (필요해지면 얇게 1개), 20 · 21 (실측·조사가 본체)
- **15단계는 자료 대신 씨앗 회수 색인** — 원문이 이미 지도 형태로 다 풀어 놓았습니다

범위를 바꾸려면 GUIDE 11.1 기준으로 판단하고 11절과 변경 이력을 함께 고칩니다.
문서를 늘리기 전에, 그 개념이 이미 만들기로 한 문서에 `[씨앗]`으로 들어갈 수 있는지 먼저 봅니다.

## 빌드

```bash
cd study
./build/build.sh 01-hardware   # 특정 문서만
./build/build.sh               # 전부 (GUIDE.md 는 제외)
```

- **pandoc 3.x 가 필요합니다.** 현재 이 머신에는 설치돼 있지 않고, `apt` 로는 2.5 가 깔려 쓸 수 없습니다.
  설치 방법은 `study/build/README.md`
- 사이드바 목차 · 현재 위치 표시 · 제목 앵커 · 테마 토글은 **템플릿이 모든 문서에 자동 적용**합니다.
  문서마다 따로 만들지 않습니다. 화면 구성을 바꾸려면 `study/build/template.html` 과 `style.css` 를 고치고 **전체 재빌드**합니다

## 하드웨어 전제

| 항목 | 값 |
|---|---|
| GPU | RTX 5090 32GB × 1 (Blackwell, sm_120) |
| 드라이버 | 580.173.02 |

- **9단계(Tensor / Pipeline Parallel)는 GPU 1대라 실습 불가** — 나머지 20단계는 가능
- 3090(Ampere)에 없는 **FP8 / NVFP4 하드웨어 가속이 있음** → 3 · 16단계에서 유리
- 4090 · 5090 에는 **NVLink 가 없음**
